apiVersion: apps/v1
kind: Deployment
metadata:
  name: syslog-meraki-gen
  namespace: synthetic-network
spec:
  replicas: 1
  selector:
    matchLabels: {app: syslog-meraki-gen}
  template:
    metadata:
      labels: {app: syslog-meraki-gen}
    spec:
      containers:
        - name: syslog-meraki-gen
          image: kennethfoo24/synthetic-netgen:latest
          args:
            - syslog-meraki
            - --target-host=elastic-agent
            - --target-port=9005
            - --topology=/app/topology/network.yaml
          resources:
            requests: {cpu: 50m, memory: 128Mi}
            limits: {memory: 256Mi}
